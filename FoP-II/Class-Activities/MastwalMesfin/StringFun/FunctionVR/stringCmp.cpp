#include"strinG.h"

int stringCmp()
{
    if(stringLen(inpuT)==stringLen(inpuTS))
    {
        return 0;
    }
    else if(stringLen(inpuT)>stringLen(inpuTS))
    {
        return 1;
    }
        return -1;
}